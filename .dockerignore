# Git and VCS
.git
.gitignore

# Terraform / local runs (large or churning; not needed to compile the provider)
**/.terraform
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/tfplan
**/fabric.identity.auto.tfvars

# Python / tests (not used in Dockerfile build)
tests
.pytest_cache
__pycache__
*.pyc
venv
.venv

# IDE / OS
.idea
.vscode
*.swp
.DS_Store
